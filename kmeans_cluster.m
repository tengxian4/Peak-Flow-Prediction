 clc;
close all;
clear all; 
data_table = readtable('data.xlsx');
flood_data =table2array(data_table);

rng default;
X = flood_data(:,1:3);
[idx,C] = kmeans(X,3);

A = [X idx]; %find the ori data with cluster number generated by the kmeans
min(A); %to find the minimum so that the following will find only the cluster number not other values.

[row, col]=find(A==1);
min(X(row,:));
max(X(row,:));

[row, col]=find(A==2);
min(X(row,:));
max(X(row,:));

[row, col]=find(A==3);
min(X(row,:));
max(X(row,:));